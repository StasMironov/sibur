import sliderType from "./slider-type";
import sliderRating from "./slider-rating";
import Tabs from './tabs';

export default {
	init() {
		Tabs.init();
		sliderType.init();
		sliderRating.init();
	}
};
